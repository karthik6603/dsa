package array;

public class RotateArray {
    public static void main(String[] args) {
        // rotate the array by k steps
        // if the array is roated to the left we have to use the reverse the array ane then the partial reversal of the parts till the k
    }
}
